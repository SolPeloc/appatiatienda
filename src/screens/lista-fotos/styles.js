import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,  
    },
    titulo:{
      paddingTop:20,
      fontSize:20,
      fontFamily: "poppins-italic",
      textAlign:"center"
    },
});